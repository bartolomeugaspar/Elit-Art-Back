services:
  - type: web
    name: elit-arte-whatsapp
    runtime: node
    region: frankfurt
    plan: starter
    buildCommand: cd whatsapp-service && npm install && npm run build
    startCommand: ./start.sh
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: FRONTEND_URL
        value: https://elit-arte.vercel.app
